import React from 'react';
import ReactDOM from 'react-dom';
import ProgressApp from './ProgressTracking';

const progressContainer = document.getElementById('react-progress-app');

ReactDOM.render(<ProgressApp />, progressContainer);